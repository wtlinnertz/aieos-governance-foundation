[pytest]
testpaths = .
pythonpath = . ..
python_files = test_*.py
python_classes = Test*
python_functions = test_*
bdd_features_base_dir = .
addopts = -v --tb=short
markers =
    slow: optional long-running checks
    cross_repo: requires sibling kit repos checked out (run in B3, not B1)
