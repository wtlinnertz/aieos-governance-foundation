#!/usr/bin/env bash
# Placeholder run.sh for the test.integration suite. See
# conformance/test.unit-suite/run.sh for the full interface contract —
# this script follows the same pattern.
set -euo pipefail

ADAPTER_REF="${1:-<not-provided>}"
echo "[test.integration-suite/run.sh — placeholder] adapter=${ADAPTER_REF}"
exit 0
