#!/usr/bin/env bash
# build.artifact-suite — placeholder. See conformance/test.unit-suite/run.sh
# for the full interface contract; the M4 conformance harness drives this.
set -euo pipefail
echo "[build.artifact-suite/run.sh — placeholder] adapter=${1:-<not-provided>}"
exit 0
